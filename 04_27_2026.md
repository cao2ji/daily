# Daily Status - April 27, 2026

## Completed

- Fixed an out-of-sync issue in `rxdata_monitor`.
  - Root cause: `rxdata_monitor` did not check `dme_ctrl_bits`, so it could mistakenly recognize traffic as DME traffic and update `dme_status` in `dln_config`.
  - Impact: either side could think `eq_train` was complete and proceed to the next stage incorrectly.
  - Fix: added a `dme_ctrl_bits` check and only update `dme_status` when `dme_ctrl_bits` matches `TX_DME_CTRL_BITS`.

- Fixed assertion error `ASSERT_T_SLEEP_POWERDOWN_TX`.
  - Root cause: there is exactly a 1 us delay between `SLEEP` and `IDDQ` in `hs_data_duplex_pwr_abort_test`, which could sometimes trigger the assertion.
  - Fix: added a 1 ns delay to avoid the assertion firing.

- Modified `afifo_checker` after Pinshiu found it could raise a false alarm during `l1_exit`.
  - Fix: disable `afifo_checker` when the checking condition is invalid, until a reset of `wen` occurs (`wen` de-asserts and then re-asserts).

## Root Caused

- Root caused `eq_train_valid` errors.
  - Root cause: AE register reads have issues when there is ongoing `eq_train`; reads can return wrong values.
  - Next step: Don will modify `link_training_vseq`.

## Ongoing

- `eq_train_valid` errors: pending Don's `link_training_vseq` modification.
- `ASSERT_RX_DATA_is_not_xz_if_dln_not_in_rx_data_en`
- `uphy_afifo_checker-AFIFO_TRKPLL_NOT_LOCKED`
- `QUEUE_EXIT`
